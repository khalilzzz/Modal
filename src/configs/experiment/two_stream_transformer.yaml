# @package _global_
# Two-Stream Spatio-Temporal Transformer — Track A (from scratch)
defaults:
  - override /model: two_stream_transformer

dataset:
  num_frames: 4

training:
  epochs: 10
  batch_size: 64
  lr: 0.0003
